#!/usr/bin/env bluejay

-- TODO: Need to assert that cleanup runs.
function cleanup()
	print("Cleaning up after failed assertion.")
end

Test:throws(error("Raise an error. This shouldn't print inside the throws function.", 3))
